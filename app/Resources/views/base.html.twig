<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {% stylesheets '@bootstrap_less' combine=true %}
            <link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
            {% endstylesheets %}
        {%  endblock stylesheets %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
    <div class="navbar navbar-inverse" role="navigation">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="sr-only">Увійти</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('_tests',{'id':-1, 'page':1}) }}">Наші тести</a></li>
                    <li><a href="{{ path('_about') }}">Про нас</a></li>
                    <li><a href="{{ path('_contacts') }}">Контакти</a></li>
                    <li><a href="{{ path('_guestbook') }}">Гостьова книга</a></li>
                </ul>
            </div>
            <div class="collapse navbar-collapse">
                {% set token=app.security.getToken() %}
                {% if token.user is defined %}
                 {% if token.getUser() is same as("anon.") %}
                  {% set loginFlag=false %}
                 {% endif %}
                 {% if token.getUser() is not same as("anon.") %}
                  {% set loginFlag=true %}
                 {% endif %}
                {% endif %}                 
                {% if token.user is not defined %}                    
                  {% set loginFlag=false %}                
                {% endif %}                 
                {% if loginFlag==false %}                    
                <form class="navbar-form navbar-right" action="{{ path('_blog_security_check')}}" method="POST">
                        <div class="form-group">
                            <input type="text" name="_username" placeholder="Username" class="form-control">
                        </div>
                        <div class="form-group">
                            <input type="password" name="_password" placeholder="Password" class="form-control">
                        </div>
                        <button type="submit" class="btn btn-success">Sign in</button>
                    </form>
                {% else %}
                <form class="navbar-form navbar-right" action="{{ path('_blog_logout')}}" method="GET">
                        {{ token.getUser().getUsername() }}
                        <button type="logout" class="btn btn-success">Sign out</button>
                    </form>
                {% endif %}
                </div><!--/.navbar-collapse -->
        </div>
    </div>

    <!-- Main jumbotron for a primary marketing message or call to action -->
    <!--<div class="jumbotron"> -->
    
    <div class="container">
     <div class="span9">
        
        {% block body %}{% endblock %}
        <div class="row">
          <div class="col-sm-6 col-md-4">

          {% block content %}

          <h1>Hello, world!</h1>
          <p>This is a template for a simple marketing or informational website. It includes a large callout called a jumbotron and three supporting pieces of content. Use it as a starting point to create something more unique.</p>
          <p><a class="btn btn-primary btn-lg" role="button" href="/tests/">Пройти тест &raquo;</a></p>
          {% endblock content %}
          </div>
        </div>

     </div> <!-- class span9-->
     <div class="row pull-right">
        <div class="col-sm-6 col-md-4">

            <h3> Останні тести:</h3>
            {{ render(controller('AcmeDemoBundle:My:latestTests')) }}
            <h3> Популярні тести:</h3>
            {{ render(controller('AcmeDemoBundle:My:popularTests')) }}
            <h3> Останні відгуки:</h3>
            {{ render(controller('AcmeDemoBundle:Post:latestPosts')) }}
            <h3> Теги:</h3>
            {{ render(controller('AcmeDemoBundle:Tags:tags')) }}
        </div>
     </div> 
    </div>

    <hr>

    <footer>
        <p>&copy; Chaban&cO 2013-2014</p>
    </footer>
    </div> <!-- /container -->


    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    {% block javascripts %}
        {% javascripts '@jquery_js' '@bootstrap_js' filter='?yui_js' combine=true %}
        <script src="{{ asset_url }}"></script>
        {% endjavascripts %}
    {% endblock javascripts %}
    

    </body>
</html>
