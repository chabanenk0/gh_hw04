{% extends "::base.html.twig" %}

{% block title "Hello " ~ name %}

{% block content %}
    <p> Hello, {{ app.security.getToken().getUser().getUsername() }}!</p>

    <a href="{{ path('_demo_secured_hello_admin', { 'name': name }) }}">Hello resource secured for <strong>admin</strong> only.</a>
{% endblock %}

{% set code = code(_self) %}
