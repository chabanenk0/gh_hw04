{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OneCaseTestQuestion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Number</th>
                <th>Numberofanswers</th>
                <th>Question</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('onecasetestquestion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.number }}</td>
                <td>{{ entity.numberOfAnswers }}</td>
                <td>{{ entity.question }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('onecasetestquestion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('onecasetestquestion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('onecasetestquestion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
